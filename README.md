Following along here: https://www.youtube.com/watch?v=KoIURitLTHQ

Can run the tests with Maven `mvn clean test`
Can package and run like so `mvn clean package && java -cp target/java-pattern-matching-1.0-SNAPSHOT.jar couryrr.github.App`